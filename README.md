# To replace image
1. Create the image
2. Run the build.html locally to load new image and enter the password - as stored in github vault
3. Change the `index.html` sigbytes value check to the new value on the local webpage
4. Copy the encrypted string into the `textarea` of the `index.html`
